apiVersion: e2e.stakater.com/v1alpha1
kind: Runner
metadata:
  name: {{ .name }}
  labels:
    sequence: {{ .sequenceLabel }}
spec:
  runnerSelector:
    matchLabels:
      app: {{ .watchRunnerLabel }}
  template:
    spec:
      containers:
        - name: runner2-job
          image: busybox
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "sleep {{ .jobDuration }} && echo done && exit 0"]
      restartPolicy: Never
