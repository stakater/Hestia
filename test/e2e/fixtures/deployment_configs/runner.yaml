apiVersion: e2e.stakater.com/v1alpha1
kind: Runner
metadata:
  name: {{ .name }}
  labels:
    app: deployment-config-runner
spec:
  deploymentSelector:
    matchLabels:
      app: e2e-deployment-config
  template:
    spec:
      containers:
        - name: deployment-config-job
          image: busybox
          imagePullPolicy: IfNotPresent
          command: ["sh", "-c", "sleep {{ .jobDuration }} && echo done && exit 0"]
      restartPolicy: Never
